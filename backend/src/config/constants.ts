export const VALID_PROTOCOLS = new Set(["HTTP", "HTTPS", "TCP", "ICMP"]);

export const HTTP_TIMEOUT_MS = 8000;
export const TCP_TIMEOUT_MS = 5000;
export const ICMP_TIMEOUT_MS = 4000;

export const HISTORY_LIMIT = 360;
